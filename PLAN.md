# 开发计划

## 阶段 0：代码库分析与计划

- 当前项目是新初始化的 Git worktree，原始仓库仅包含需求 prompt。
- 目标是从零创建 SpringBoot3 + Vue3 + MySQL 的数据库课程小型商城平台。
- 核心验收重点：数据库约束、订单事务、JWT 权限、前后端闭环、文档和接口测试素材。

## 阶段 1：数据库与后端基础

- 创建 Spring Boot 3 后端工程。
- 配置 MySQL、MyBatis-Plus、统一响应、统一异常、JWT 拦截器、管理员权限校验。
- 创建 `schema.sql`、`data.sql`，包含主键、外键、唯一约束、索引和初始化数据。

## 阶段 2：后端核心接口

- 实现用户、分类、商品、购物车、地址、订单、管理员管理、统计接口。
- 订单创建必须使用 `@Transactional`，并使用安全库存扣减 SQL 防止负库存。

## 阶段 3：前端页面

- 创建 Vue3 + Vite + Element Plus 前端工程。
- 实现用户端和管理员端页面、Pinia 登录状态、Axios 拦截器、路由守卫。

## 阶段 4：测试与修复

- 运行可行的前端构建检查。
- 后端因当前 WSL 缺少 Java 17/Maven，优先做静态一致性检查；如提供工具链再执行 Maven 构建。

## 阶段 5：文档与交付

- 完成 `README.md`、`docs/` 实验报告素材、`api-test.http`。
- 输出运行方式、测试方式、已知限制和下一步建议。

